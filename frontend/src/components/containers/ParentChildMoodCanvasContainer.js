import React from 'react';
import { View, Text } from 'react-native';

const ParentChildMoodCanvasContainer = () => {
  return (
    <View>
      <Text style={{ fontSize: 18, fontWeight: '600' }}>Mood Canvas</Text>
      {/* Add mood UI here */}
    </View>
  );
};

export default ParentChildMoodCanvasContainer;
