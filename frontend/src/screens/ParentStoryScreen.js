import ParentStoryContainer from '../components/containers/ParentStoryContainer';

const ParentStoryScreen = ({navigation}) => {
  return ( <ParentStoryContainer navigation={navigation} /> );
};

export default ParentStoryScreen;
