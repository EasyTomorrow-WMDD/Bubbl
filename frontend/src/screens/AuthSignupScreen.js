import AuthSignupContainer from '../components/containers/AuthSignupContainer';

const AuthSignupScreen = ({navigation}) => {
  return ( <AuthSignupContainer navigation={navigation} /> );
};

export default AuthSignupScreen;
