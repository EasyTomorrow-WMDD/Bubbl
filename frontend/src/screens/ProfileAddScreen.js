import ProfileAddContainer from '../components/containers/ProfileAddContainer';

const ProfileAddScreen = ({navigation}) => {
  return ( <ProfileAddContainer navigation={navigation} /> );
};

export default ProfileAddScreen;
