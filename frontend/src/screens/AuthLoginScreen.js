import AuthLoginContainer from '../components/containers/AuthLoginContainer';

const AuthLoginScreen = ({navigation}) => {
  return ( <AuthLoginContainer navigation={navigation} /> );
};

export default AuthLoginScreen;
