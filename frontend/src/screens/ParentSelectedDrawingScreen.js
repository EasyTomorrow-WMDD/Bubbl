import ParentSelectedDrawingContainer from '../components/containers/ParentSelectedDrawingContainer';

const ParentSelectedDrawingScreen = ({ navigation, route }) => {
  return (
    <ParentSelectedDrawingContainer navigation={navigation} route={route} />
  );
};

export default ParentSelectedDrawingScreen;